# Infrastructure Report

## Operating System

The cloud environment is running **Ubuntu 24.04.4 LTS**.

## Kernel Version

The Linux kernel version is:

`6.8.0-xx-generic`

## CPU

The CPU information shows:

**Intel Xeon E312xx (Sandy Bridge, IBRS update)**

## Number of CPU Cores

The system has:

**1 CPU core**

## Total RAM

The system has approximately:

**1.9 GB of total memory**

Based on the `free -h` command, approximately **871 MB was free** and **786 MB was available** during the system check.

## Disk Capacity

Based on the `df -h` command, the main filesystem has:

- **Total Size:** 19 GB
- **Used Space:** 5.4 GB
- **Available Space:** 13 GB
- **Usage:** 30%

## Mounted File Systems

The mounted file systems include:

- `/` — Main filesystem
- `/boot` — Boot files
- `/boot/efi` — EFI boot partition
- `/run` — Runtime files
- `/dev/shm` — Shared memory

The main filesystem is mounted from `/dev/vda1` and uses the **ext4** filesystem.

## Hostname

The hostname of the cloud server is:

`ubuntu`

## IP Address

The cloud server has the following IP addresses:

`172.30.1.2`

`172.17.0.1`
